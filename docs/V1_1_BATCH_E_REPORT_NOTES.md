# Report workspace notes

The E.1 renderer presents only already-authoritative export metadata. It does not create a second report model. The active-session PDF guard remains in `applicationShell.ts`, and PDF execution remains delegated through the existing privileged action.
