# Batch B.4 Notes

This batch intentionally does not add, remove, extrapolate, or synthesize pump capability data. It only exposes already-present verified capability points for session-draft editing. Local-loss calibration contracts remain unchanged.
