# v1.1 Fast-Track Mode

Development is intentionally lightweight and product-focused.

Required during feature work:
- keep engineering calculations unchanged unless a real defect is demonstrated;
- preserve licensing and persistence behavior;
- pass typecheck and automated tests;
- use Windows package smoke when desktop packaging/runtime is affected.

Everything else is optional process overhead and should not block visible product progress.
