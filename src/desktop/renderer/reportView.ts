import { TOLUE_DESIGN_TOKENS } from './designSystem';
import { appendEngineeringSectionHeader, styleEngineeringButton, styleEngineeringSection } from './engineeringPanelStyle';
import type { ReportExportPresentation } from './reportPresentation';

export interface ReportExportActions { readonly exportActiveEngineeringPdf:()=>Promise<unknown>; }
export interface PdfExportStatusPresentation { readonly status:'SUCCESS'|'CANCELLED'|'FAILED'|'REJECTED'; readonly message:string; }
function isRecord(value:unknown):value is Record<string,unknown>{return typeof value==='object'&&value!==null&&!Array.isArray(value);}
export function presentEngineeringPdfExportResponse(report:Readonly<ReportExportPresentation>,response:unknown):Readonly<PdfExportStatusPresentation>{if(!isRecord(response))throw new Error('REPORT-PDF-RESPONSE-SHAPE-001');const status=response.status;if(status!=='SUCCESS'&&status!=='CANCELLED'&&status!=='FAILED'&&status!=='REJECTED')throw new Error('REPORT-PDF-RESPONSE-STATUS-001');const errorCode=typeof response.errorCode==='string'?response.errorCode:null;if(status==='REJECTED')return Object.freeze({status,message:`درخواست صدور PDF رد شد${errorCode?` · ${errorCode}`:''}`});if(response.runId!==report.runId||response.engineVersion!==report.engineVersion||response.inputSnapshotHash!==report.inputSnapshotHash)throw new Error('REPORT-PDF-RESPONSE-IDENTITY-001');if(status==='CANCELLED')return Object.freeze({status,message:'صدور PDF توسط کاربر لغو شد؛ فایلی نوشته نشد.'});if(status==='FAILED')return Object.freeze({status,message:`صدور PDF انجام نشد${errorCode?` · ${errorCode}`:''}`});const savedFileName=typeof response.savedFileName==='string'&&response.savedFileName.trim()?response.savedFileName:null;const bytesWritten=typeof response.bytesWritten==='number'&&Number.isFinite(response.bytesWritten)&&response.bytesWritten>0?response.bytesWritten:null;if(!savedFileName||bytesWritten===null||errorCode!==null)throw new Error('REPORT-PDF-RESPONSE-SUCCESS-001');return Object.freeze({status,message:`PDF با موفقیت ذخیره شد: ${savedFileName} · ${bytesWritten.toLocaleString('fa-IR')} بایت`});}
function appendFormatCard(root:HTMLElement,titleText:string,mediaType:string,descriptionText:string):void{const card=document.createElement('article');Object.assign(card.style,{padding:'10px 12px',background:'rgba(12,23,30,.74)',border:`1px solid ${TOLUE_DESIGN_TOKENS.color.border}`,borderRadius:TOLUE_DESIGN_TOKENS.radius.sm});const title=document.createElement('strong');title.textContent=titleText;const description=document.createElement('p');description.textContent=descriptionText;Object.assign(description.style,{margin:'5px 0',color:TOLUE_DESIGN_TOKENS.color.textMuted});const media=document.createElement('small');media.textContent=mediaType;media.style.direction='ltr';card.append(title,description,media);root.appendChild(card);}

export function renderReportView(root:HTMLElement,report?:Readonly<ReportExportPresentation>,actions?:Readonly<ReportExportActions>):void{
  const panel=document.createElement('section');panel.setAttribute('aria-label','گزارش و خروجی');styleEngineeringSection(panel,true);appendEngineeringSectionHeader(panel,'گزارش مهندسی و خروجی‌ها','هویت Run، Engine Version و Snapshot پیش از صدور کنترل می‌شوند.','REPORT EXPORT');const body=document.createElement('div');body.style.padding='12px';panel.appendChild(body);
  if(!report){const empty=document.createElement('div');empty.setAttribute('role','status');empty.textContent='گزارش هنوز آماده نیست. ابتدا تحلیل معتبر را اجرا کنید.';Object.assign(empty.style,{padding:'12px',background:TOLUE_DESIGN_TOKENS.color.surfaceMuted,borderRadius:TOLUE_DESIGN_TOKENS.radius.sm,color:TOLUE_DESIGN_TOKENS.color.textMuted});body.appendChild(empty);root.appendChild(panel);return;}
  const readiness=document.createElement('div');readiness.setAttribute('role','status');readiness.textContent='READY FOR EXPORT · گزارش به Run فعال و Snapshot فعلی متصل است.';Object.assign(readiness.style,{marginBottom:'10px',padding:'9px 11px',border:`1px solid ${TOLUE_DESIGN_TOKENS.color.statusNominal}`,borderRadius:TOLUE_DESIGN_TOKENS.radius.sm,color:TOLUE_DESIGN_TOKENS.color.statusNominal,direction:'ltr',fontWeight:'700'});body.appendChild(readiness);
  const formats=document.createElement('div');Object.assign(formats.style,{display:'grid',gridTemplateColumns:'repeat(3,minmax(0,1fr))',gap:'8px'});appendFormatCard(formats,'PDF',report.pdfRequest.mediaType,'نسخه قابل ارائه و آرشیو از Run فعال');appendFormatCard(formats,'HTML',report.htmlMediaType,'نمای ساخت‌یافته گزارش طبق قرارداد Core');appendFormatCard(formats,'JSON',report.jsonMediaType,'داده ساخت‌یافته برای رهگیری و تبادل');body.appendChild(formats);
  const traceability=document.createElement('details');traceability.style.marginTop='10px';const traceSummary=document.createElement('summary');traceSummary.textContent='هویت و رهگیری گزارش';traceSummary.style.cursor='pointer';const meta=document.createElement('div');Object.assign(meta.style,{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(180px,1fr))',gap:'8px',marginTop:'8px'});for(const [labelText,valueText] of [['Run ID',report.runId],['Engine',report.engineVersion],['Snapshot',report.inputSnapshotHash],['Locale',report.reportLocale],['Direction',report.reportDirection],['Bundle method',report.bundleMethod]] as const){const card=document.createElement('article');Object.assign(card.style,{padding:'8px 10px',background:TOLUE_DESIGN_TOKENS.color.surfaceMuted,borderRadius:TOLUE_DESIGN_TOKENS.radius.sm});const label=document.createElement('small');label.textContent=labelText;label.style.color=TOLUE_DESIGN_TOKENS.color.textMuted;const value=document.createElement('div');value.textContent=valueText;Object.assign(value.style,{marginTop:'3px',overflowWrap:'anywhere',direction:'ltr',fontFamily:TOLUE_DESIGN_TOKENS.typography.monoFamily,fontSize:TOLUE_DESIGN_TOKENS.typography.fontSizeXs});card.append(label,value);meta.appendChild(card);}traceability.append(traceSummary,meta);body.appendChild(traceability);
  const exportArea=document.createElement('div');Object.assign(exportArea.style,{marginTop:'12px',paddingTop:'10px',borderTop:`1px solid ${TOLUE_DESIGN_TOKENS.color.border}`});const exportHint=document.createElement('p');exportHint.textContent='PDF فقط از قرارداد آماده‌شده توسط Engineering Core و Run فعال صادر می‌شود؛ Renderer مستقیماً فایل‌سیستم را کنترل نمی‌کند.';exportHint.style.color=TOLUE_DESIGN_TOKENS.color.textMuted;const exportStatus=document.createElement('p');exportStatus.setAttribute('aria-live','polite');exportStatus.style.color=TOLUE_DESIGN_TOKENS.color.textMuted;const pdf=document.createElement('button');pdf.type='button';pdf.textContent='صدور PDF Run فعال';styleEngineeringButton(pdf,true);pdf.disabled=!actions;if(!actions){exportStatus.textContent='عملیات صدور PDF در این Session در دسترس نیست.';}else{pdf.addEventListener('click',()=>{pdf.disabled=true;pdf.setAttribute('aria-busy','true');pdf.textContent='در حال صدور PDF…';exportStatus.textContent='در حال صدور PDF Run فعال…';void actions.exportActiveEngineeringPdf().then(response=>{const presentation=presentEngineeringPdfExportResponse(report,response);exportStatus.textContent=presentation.message;exportStatus.style.color=presentation.status==='SUCCESS'?TOLUE_DESIGN_TOKENS.color.statusNominal:presentation.status==='FAILED'||presentation.status==='REJECTED'?TOLUE_DESIGN_TOKENS.color.statusCritical:TOLUE_DESIGN_TOKENS.color.statusWarning;}).catch(()=>{exportStatus.textContent='پاسخ صدور PDF معتبر نیست یا هویت Run/Hash با Session فعال تطابق ندارد.';exportStatus.style.color=TOLUE_DESIGN_TOKENS.color.statusCritical;}).finally(()=>{pdf.disabled=false;pdf.removeAttribute('aria-busy');pdf.textContent='صدور PDF Run فعال';});});}exportArea.append(exportHint,pdf,exportStatus);body.appendChild(exportArea);root.appendChild(panel);
}
