# Renderer UX direction for v1.1

Primary objective: make the desktop application read like an engineering workflow rather than an internal state machine.

Internal states may remain unchanged in code, but user-visible copy should be concise Persian guidance. Navigation should make the active engineering step obvious. Secondary tools should not compete visually with the main Project → Inputs → Analysis → Results → Report flow.
